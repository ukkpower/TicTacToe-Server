
namespace Scripts.Networking {
	class Tags {

		public enum Tag {
			SET_NAME,
            CREATE_MATCH,
			MATCHED_CREATED,
            JOIN_MATCH,
			MATCH_STARED,
            GOT_MATCH,
			SLATE_TAKEN,
			SERVER_CONFIRM_SLATE_TAKEN
		}

	}
}
